{
  "name": "chat-app",
  "version": "1.0.0",
  "description": "Full-stack chat application with React, Express, and Socket.IO",
  "scripts": {
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "build": "cd frontend && npm run build && xcopy /E /I /Y frontend\\dist backend\\dist",
    "start": "cd backend && npm start"
  },
  "keywords": ["chat", "react", "socket.io", "express"],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}
